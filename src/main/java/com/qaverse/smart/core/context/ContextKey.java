package com.qaverse.smart.core.context;

public enum ContextKey {

    DRIVER,

    ACTION_REQUEST,
    ACTION_RESULT,

    FAILURE_CONTEXT,

    OBSERVATION_RESULT,
    VALIDATION_RESULT,
    RECOVERY_RESULT,
    RETRY_RESULT,

    CURRENT_ATTEMPT,
    MAX_ATTEMPTS,

    START_TIME,
    END_TIME,

    EXECUTION_ID,

    CUSTOM
}